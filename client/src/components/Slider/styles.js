import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles((theme) => ({

img: {
  maxWidth: "15vw",
  maxHeight: "15vh",
  minWidth: "15vw",
  minHeight: "15vh",
},
imgSide: {
  maxWidth: "9vw",
  maxHeight: "9vh",
  minWidth: "9vw",
  minHeight: "9vh",
},
}));

export { useStyles };