import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles((theme) => ({
  imgContent: {
    minWidth: "100%",
    minHeight: "100%",
    padding: "1%",
    margin: "1%",
  },
}));

export { useStyles };
