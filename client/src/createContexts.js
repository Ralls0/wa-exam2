import React from 'react';

const LoggedInMode = React.createContext();
const UserInfoMode = React.createContext();
const MemeImages = React.createContext();
const MemeFonts = React.createContext();

export { LoggedInMode, UserInfoMode, MemeImages, MemeFonts };